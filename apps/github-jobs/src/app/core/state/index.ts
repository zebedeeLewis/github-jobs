import {
  MUST_LOAD_NEXT_PAGE,
  MUST_NOT_LOAD_NEXT_PAGE,
  create,
  getThemeScheme,
  setThemeScheme,
  getMustLoadNextPage,
  setMustLoadNextPage,
  getCurrentPage,
  setCurrentPage,
  getJobs,
  setJobs,
} from './app-core-state'
import type { Model } from './app-core-state'

export {
  MUST_LOAD_NEXT_PAGE,
  MUST_NOT_LOAD_NEXT_PAGE,
  create,
  getThemeScheme,
  setThemeScheme,
  getMustLoadNextPage,
  setMustLoadNextPage,
  getCurrentPage,
  setCurrentPage,
  getJobs,
  setJobs,
}
export type { Model }
