import {
  DARK_MODE_ON,
  DARK_MODE_OFF,
  PageLoad,
  create,
  getDarkModeToggle,
  setDarkModeToggle,
  getPageLoad,
  setPageLoad,
  getCurrentPage,
  setCurrentPage,
  getJobs,
  setJobs,
  isDarkModeOn,
  isLoadNeeded,
  isLoadingJobs,
} from './app-core-state'
import type { Model } from './app-core-state'

export {
  DARK_MODE_ON,
  DARK_MODE_OFF,
  PageLoad,
  create,
  getDarkModeToggle,
  setDarkModeToggle,
  getPageLoad,
  setPageLoad,
  getCurrentPage,
  setCurrentPage,
  getJobs,
  setJobs,
  isDarkModeOn,
  isLoadNeeded,
  isLoadingJobs,
}

export type { Model }
