import {
  DARK_MODE_ON,
  DARK_MODE_OFF,
  LOADING,
  NONE_LOADED,
  LOADING_DONE,
  LOADING_FAILED,
  create,
  getDarkModeToggle,
  setDarkModeToggle,
  getPageLoad,
  setPageLoad,
  getCurrentPage,
  setCurrentPage,
  getJobs,
  setJobs,
  getFilters,
  setFilters,
  isLoadingJobs,
} from './state'
import type { Model, PageLoad } from './state'

export {
  DARK_MODE_ON,
  DARK_MODE_OFF,
  LOADING,
  NONE_LOADED,
  LOADING_DONE,
  LOADING_FAILED,
  create,
  getDarkModeToggle,
  setDarkModeToggle,
  getPageLoad,
  setPageLoad,
  getCurrentPage,
  setCurrentPage,
  getJobs,
  setJobs,
  getFilters,
  setFilters,
  isLoadingJobs,
}

export type { Model, PageLoad }
