import { presentationUiInstruction } from './presentation-ui--instruction'

describe('presentationUiInstruction', () => {
  it('should work', () => {
    expect(presentationUiInstruction()).toEqual(
      'presentation-ui--instruction'
    )
  })
})
