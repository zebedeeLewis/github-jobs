import { presentationUiUtils } from './presentation-ui--utils'

describe('presentationUiUtils', () => {
  it('should work', () => {
    expect(presentationUiUtils()).toEqual('presentation-ui--utils')
  })
})
