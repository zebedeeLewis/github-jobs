import { domainDataAccessObject } from './domain--data-access-object'

describe('domainDataAccessObject', () => {
  it('should work', () => {
    expect(domainDataAccessObject()).toEqual(
      'domain--data-access-object'
    )
  })
})
