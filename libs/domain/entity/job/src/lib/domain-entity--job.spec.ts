describe('Job Entity', () => {
	describe('Model creation', () => {
    it( 'does stuff.',
      () => {
        // Arrange
        const expected = true

        // Act
        const recieved = true

        // Assert
        expect(recieved).toBe(expected)
      }
    )
	})
})
